package com.tencent.devops.scm.provider.git.gitee.enums;

enum class GiteeActionDesc(val value: String) {
    SOURCE_BRANCH_CHANGED("source_branch_changed"),
    TARGET_BRANCH_CHANGED("target_branch_changed");
}